bundle:
  name: "sensor-app-2"

sync:
  include:
    - .build

artifacts:
  default:
    build: uv run apx build

resources:
  apps:
    sensor-app-2-app:
      name: "sensor-app-2"
      description: "Sensor monitoring app with mock data"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true